# 🚀 Быстрый запуск проекта PizzaOrder

## ✅ Проект запущен!

### 📱 Frontend (React Native + Expo)

**Сервер разработки запущен!**

Откройте один из вариантов:
- **Web**: Нажмите `w` в терминале или откройте браузер на http://localhost:8081
- **Android**: Нажмите `a` (требуется Android Studio/эмулятор)
- **iOS**: Нажмите `i` (требуется macOS и Xcode)

Или отсканируйте QR-код в терминале приложением Expo Go на мобильном устройстве.

### 🔧 Backend (NestJS)

**Backend запущен на:** `http://localhost:3000`

API endpoints:
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/pizzas` - Список пицц
- `GET /api/pizzas/:id` - Детали пиццы
- `POST /api/orders` - Создать заказ (требуется авторизация)
- `GET /api/admin/orders` - Все заказы (требуется роль ADMIN)

## ⚠️ Важно!

**Для полной функциональности backend требуется:**

1. **PostgreSQL база данных**
   - Установите PostgreSQL
   - Создайте базу данных `pizzaorder`
   - Настройте `.env` в папке `backend/`:
     ```env
     DATABASE_URL="postgresql://user:password@localhost:5432/pizzaorder?schema=public"
     JWT_SECRET=your-super-secret-jwt-key
     JWT_EXPIRES_IN=7d
     PORT=3000
     ```

2. **Выполните миграции:**
   ```bash
   cd backend
   npx prisma migrate dev --name init
   npx prisma generate
   ```

**Пока backend не подключен к БД:**
- Frontend использует моковые данные для пицц
- Авторизация и заказы не будут работать
- Но вы можете увидеть UI и навигацию

## 🎯 Что можно сделать сейчас:

1. ✅ Просмотреть список пицц на главной странице
2. ✅ Использовать поиск пицц
3. ✅ Открыть детали пиццы
4. ✅ Добавить пиццу в корзину (сохраняется локально)
5. ✅ Просмотреть корзину
6. ✅ Попробовать оформить заказ (покажет ошибку без БД)

## 🔄 Для подключения backend:

1. Установите PostgreSQL
2. Создайте `.env` файл в `backend/` с настройками БД
3. Выполните миграции
4. Перезапустите backend

После этого все функции заработают полностью!

