# 🚀 Быстрая инструкция по сборке Android приложения

## Способ 1: EAS Build (Самый простой) ⭐

### Шаг 1: Установите EAS CLI
```bash
npm install -g eas-cli
```

### Шаг 2: Войдите в аккаунт Expo
```bash
eas login
```
(Создайте аккаунт на https://expo.dev если его нет)

### Шаг 3: Настройте проект
```bash
cd pizzaorder
eas build:configure
```

### Шаг 4: Соберите APK для тестирования
```bash
npm run build:android
```
или
```bash
eas build --platform android --profile preview
```

### Шаг 5: Дождитесь завершения сборки
Сборка займет 10-15 минут. В конце вы получите ссылку для скачивания APK файла.

### Шаг 6: Установите APK на Android устройство
1. Скачайте APK файл
2. Передайте его на Android устройство
3. Включите "Установка из неизвестных источников" в настройках
4. Установите APK файл

---

## Способ 2: Локальная сборка (Требует Android Studio)

### Требования:
- Android Studio
- JDK 17+
- Android SDK

### Команды:
```bash
# Создать нативный проект
npx expo prebuild --platform android

# Собрать APK
cd android
./gradlew assembleRelease
```

APK будет в: `android/app/build/outputs/apk/release/app-release.apk`

---

## Способ 3: Тестирование через Expo Go (Быстро)

```bash
npm start
```

Отсканируйте QR-код в приложении Expo Go на Android устройстве.

---

## 📝 Важные заметки:

1. **Обновление версии**: Перед каждой новой сборкой увеличьте `versionCode` в `app.json`
2. **Иконки**: Убедитесь что файлы `assets/icon.png` и `assets/adaptive-icon.png` существуют
3. **Подпись**: Для публикации в Google Play нужна подпись приложения

---

## 🎯 Рекомендуемый способ: EAS Build

Это самый простой и надежный способ получить готовый APK файл без установки Android Studio.

