# 🌐 Как открыть Web-версию приложения

## Проблема:
Вы видите JSON манифест вместо приложения - это потому что открыли корневой URL.

## ✅ Решение - Правильный URL:

### Способ 1: Через терминал (Рекомендуется)

В терминале, где запущен `expo start`, **нажмите клавишу `w`**:
```
Press w │ open web
```

Это автоматически откроет правильный URL для web-версии.

### Способ 2: Откройте правильный URL вручную

Вместо `http://localhost:8082/` откройте:
```
http://localhost:8082/?platform=web
```

Или:
```
http://localhost:8082/?platform=web&dev=true
```

### Способ 3: Используйте команду

В новом терминале выполните:
```bash
cd c:\projects\pizzaorder
npm run web
```

Это специальная команда для запуска web-версии.

## 🎯 Что делать прямо сейчас:

1. **В терминале Expo нажмите `w`** - это самый простой способ!
2. Если это не помогло, откройте в браузере: `http://localhost:8082/?platform=web`

## ⚠️ Если все еще видите JSON:

Попробуйте открыть bundle напрямую:
```
http://localhost:8082/index.ts.bundle?platform=web
```

Но лучше использовать `w` в терминале - это гарантированно откроет правильную версию!

