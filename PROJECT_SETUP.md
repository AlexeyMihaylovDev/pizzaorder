# 🍕 PizzaOrder - Инструкция по запуску проекта

## 📋 Обзор

Проект PizzaOrder - это полнофункциональное приложение для заказа пиццы, работающее на Web, iOS и Android.

### Структура проекта

```
pizzaorder/
├── app/              # Frontend (React Native + Expo)
├── backend/         # Backend API (NestJS + Prisma + PostgreSQL)
└── README.md        # Основная документация
```

## 🚀 Быстрый старт

### 1. Настройка Frontend

```bash
cd pizzaorder

# Установка зависимостей (уже установлены)
npm install

# Создайте файл .env
echo "EXPO_PUBLIC_API_URL=http://localhost:3000/api" > .env

# Запуск приложения
npm start
```

Выберите платформу:
- `w` - Web
- `a` - Android
- `i` - iOS

### 2. Настройка Backend

```bash
cd backend

# Установка зависимостей (уже установлены)
npm install

# Настройте базу данных PostgreSQL
# Создайте файл .env на основе .env.example:
# DATABASE_URL="postgresql://user:password@localhost:5432/pizzaorder?schema=public"
# JWT_SECRET=your-super-secret-jwt-key
# JWT_EXPIRES_IN=7d
# PORT=3000

# Создайте миграции базы данных
npx prisma migrate dev --name init

# Сгенерируйте Prisma Client
npx prisma generate

# Запуск backend
npm run start:dev
```

Backend будет доступен на `http://localhost:3000`

## 📱 Функциональность

### Frontend (React Native + Expo)

✅ **Реализовано:**
- Главный экран со списком пицц
- Поиск пицц
- Детальная страница пиццы
- Корзина с сохранением в AsyncStorage
- Оформление заказа
- Авторизация и регистрация
- Админ-панель
  - Управление заказами
  - Управление меню (CRUD пицц)

🔄 **В разработке:**
- WebSocket уведомления
- Push-уведомления
- Интеграция платежей (Stripe/YooKassa)

### Backend (NestJS)

✅ **Реализовано:**
- JWT аутентификация
- CRUD операции для пицц
- Управление заказами
- Админ-панель API
- Валидация данных
- Prisma ORM с PostgreSQL

🔄 **В разработке:**
- WebSocket для real-time уведомлений
- Swagger документация
- Интеграция платежей

## 🔐 Тестовые данные

### Создание админ-пользователя

После запуска backend, зарегистрируйте пользователя с ролью ADMIN через API:

```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "password123",
    "name": "Admin",
    "role": "ADMIN"
  }'
```

Или через Postman/Insomnia.

## 🧪 Тестирование

### Frontend

```bash
cd pizzaorder
npm test  # (когда будут добавлены тесты)
```

### Backend

```bash
cd backend
npm run test        # Unit-тесты
npm run test:e2e    # E2E-тесты
```

## 📦 Сборка для продакшена

### Frontend - Web

```bash
cd pizzaorder
npx expo export:web
```

Затем разверните папку `web-build` на Vercel/Netlify.

### Frontend - Mobile

```bash
# Android
npx expo build:android

# iOS
npx expo build:ios
```

### Backend

```bash
cd backend
npm run build
npm run start:prod
```

Или используйте Docker/PMA2 для управления процессом.

## 🐛 Решение проблем

### Backend не запускается

1. Проверьте, что PostgreSQL запущен
2. Проверьте `.env` файл с правильными данными БД
3. Выполните миграции: `npx prisma migrate dev`

### Frontend не подключается к API

1. Убедитесь, что backend запущен на порту 3000
2. Проверьте `EXPO_PUBLIC_API_URL` в `.env`
3. Проверьте CORS настройки в backend

### Ошибки Prisma

1. Выполните: `npx prisma generate`
2. Проверьте подключение к БД: `npx prisma db pull`

## 📚 Дополнительная документация

- [Frontend README](./README.md)
- [Backend README](./backend/README.md)
- [Expo Documentation](https://docs.expo.dev/)
- [NestJS Documentation](https://docs.nestjs.com/)
- [Prisma Documentation](https://www.prisma.io/docs/)

## 👥 Разработка

Проект поддерживает:
- ✅ TypeScript
- ✅ Hot Reload
- ✅ Linting (ESLint)
- ✅ Code formatting (Prettier)

## 📝 TODO

- [ ] Добавить WebSocket уведомления
- [ ] Интегрировать платежи
- [ ] Добавить unit-тесты
- [ ] Добавить E2E-тесты
- [ ] Настроить CI/CD
- [ ] Добавить Swagger документацию
- [ ] Оптимизировать производительность
- [ ] Добавить аналитику

## 🎉 Готово к использованию!

Проект готов к разработке и тестированию. Начните с запуска backend, затем frontend, и приступайте к работе!

