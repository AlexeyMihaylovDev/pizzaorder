# 🔧 Устранение ошибок

## Ошибка: Failed to load resource: 500 (Internal Server Error)

### Решение 1: Установка недостающих зависимостей ✅

Проблема была решена установкой обязательных зависимостей для `expo-router`:

```bash
npm install expo-constants expo-linking react-native-worklets --legacy-peer-deps
```

### Решение 2: Перезапуск сервера разработки

После установки зависимостей перезапустите сервер:

1. Остановите текущий процесс (Ctrl+C)
2. Очистите кэш:
   ```bash
   npx expo start --clear
   ```

### Решение 3: Проверка импортов

Убедитесь, что все импорты корректны:
- `expo-router` требует `expo-constants` и `expo-linking`
- `react-native-reanimated` требует `react-native-worklets`

### Решение 4: Если проблема сохраняется

1. Удалите `node_modules` и переустановите:
   ```bash
   rm -rf node_modules
   npm install --legacy-peer-deps
   ```

2. Очистите кэш Metro:
   ```bash
   npx expo start --clear
   ```

3. Проверьте конфигурацию в `app.json`:
   - Должен быть `"plugins": ["expo-router"]`
   - Должен быть `"bundler": "metro"` в секции `web`

## Другие возможные ошибки

### MIME type error

Если видите ошибку с MIME типом, это обычно означает, что:
- Сервер вернул JSON вместо JavaScript bundle
- Проверьте консоль сервера на наличие ошибок компиляции

### Ошибки типов TypeScript

Если есть ошибки типов:
```bash
npx tsc --noEmit
```

### Проблемы с React Native Paper

Убедитесь, что используете совместимые версии:
```bash
npx expo install react-native-paper --check
```

## Проверка работоспособности

После исправления ошибок проверьте:

1. ✅ Зависимости установлены: `npm list expo-constants expo-linking`
2. ✅ Сервер запускается без ошибок
3. ✅ Bundle компилируется успешно
4. ✅ Приложение открывается в браузере/эмуляторе

## Если ничего не помогает

1. Удалите `.expo` папку
2. Переустановите зависимости с `--legacy-peer-deps`
3. Запустите с флагом `--clear`
4. Проверьте версию Node.js (рекомендуется 18+)

