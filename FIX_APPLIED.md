# ✅ Исправление применено!

## Что было исправлено:

1. ✅ **Установлены недостающие зависимости:**
   - `expo-constants` - обязательная для expo-router
   - `expo-linking` - обязательная для expo-router  
   - `react-native-worklets` - обязательная для react-native-reanimated

2. ✅ **Исправлен компонент PizzaCard:**
   - Удален неправильный `e.stopPropagation()` (не работает в React Native)
   - Кнопка перенесена в `Card.Actions` для правильной работы

## Что делать дальше:

### Перезапустите сервер разработки:

```bash
# Остановите текущий процесс (Ctrl+C в терминале Expo)

# Запустите с очисткой кэша:
npx expo start --clear

# Или просто:
npm start -- --clear
```

### Затем выберите платформу:
- Нажмите `w` для Web
- Или отсканируйте QR-код для мобильного устройства

## Проверка:

После перезапуска ошибка `Failed to load resource: 500` должна исчезнуть.

Приложение должно загрузиться и показать:
- ✅ Главную страницу с пиццами
- ✅ Поиск работает
- ✅ Корзина работает
- ✅ Навигация работает

## Если проблема сохраняется:

1. Проверьте консоль браузера на наличие других ошибок
2. Проверьте терминал Expo на ошибки компиляции
3. Убедитесь, что все зависимости установлены:
   ```bash
   npm list expo-constants expo-linking react-native-worklets
   ```

Все должно работать! 🚀

