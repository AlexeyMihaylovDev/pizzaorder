# 🍕 PizzaOrder

Приложение для заказа пиццы на React Native + Expo с поддержкой Web, iOS и Android.

## 📋 Возможности

- 📱 **Кроссплатформенность**: работает на Web, iOS и Android
- 🛒 **Корзина**: добавление товаров с сохранением в AsyncStorage
- 👤 **Авторизация**: вход и регистрация пользователей
- 🍕 **Меню пицц**: просмотр каталога с поиском
- 💳 **Оформление заказа**: с указанием адреса доставки
- 👨‍💼 **Админ-панель**: управление заказами и меню
- 🔔 **Уведомления**: для продавца и клиентов (в разработке)

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск приложения

```bash
# Запуск на всех платформах с выбором
npm start

# Или конкретная платформа:
npm run web      # Web-версия
npm run android  # Android
npm run ios      # iOS (требуется macOS)
```

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
EXPO_PUBLIC_API_URL=http://localhost:3000/api
```

## 📁 Структура проекта

```
pizzaorder/
├── app/                 # Экраны приложения (Expo Router)
│   ├── index.tsx       # Главная страница (список пицц)
│   ├── cart.tsx        # Корзина
│   ├── checkout.tsx    # Оформление заказа
│   ├── login.tsx       # Авторизация
│   ├── pizza/[id].tsx  # Детали пиццы
│   └── admin/          # Панель администратора
│       ├── index.tsx   # Дашборд
│       ├── orders.tsx  # Управление заказами
│       └── menu.tsx    # Управление меню
├── components/          # Переиспользуемые компоненты
│   ├── PizzaCard.tsx   # Карточка пиццы
│   └── CartItem.tsx    # Элемент корзины
├── contexts/           # React Context
│   ├── CartContext.tsx # Контекст корзины
│   └── AuthContext.tsx # Контекст авторизации
├── services/           # API-сервисы
│   └── api.ts         # HTTP-клиент (axios)
├── types/             # TypeScript типы
│   └── index.ts       # Интерфейсы
└── assets/            # Изображения и иконки
```

## 🛠 Технологии

- **React Native** + **Expo** - кроссплатформенная разработка
- **Expo Router** - файловая маршрутизация
- **TypeScript** - типизация
- **React Native Paper** - UI-компоненты
- **Axios** - HTTP-запросы
- **AsyncStorage** - локальное хранилище
- **React Context** - управление состоянием

## 🔐 Админ-панель

Для доступа к админ-панели необходимо:
1. Зарегистрировать пользователя с ролью `admin` (через backend)
2. Войти в систему
3. Перейти в раздел `/admin`

## 📦 Backend

Backend находится в отдельной папке `backend/` (NestJS + Prisma + PostgreSQL).

См. документацию в `backend/README.md`

## 🧪 Тестирование

```bash
# Unit-тесты (в разработке)
npm test

# E2E-тесты (в разработке)
npm run test:e2e
```

## 📱 Сборка для продакшена

### Web

```bash
npx expo export:web
```

### Android

```bash
npx expo build:android
```

### iOS

```bash
npx expo build:ios
```

## 🐛 Известные проблемы

- Временные моковые данные для пицц (до подключения backend)
- WebSocket уведомления в разработке
- Push-уведомления в разработке

## 📝 Лицензия

MIT

## 👥 Авторы

PizzaOrder Team

